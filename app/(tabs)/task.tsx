import { StyleSheet, Text, View } from "react-native";
import React from "react";

const task = () => {
  return (
    <View>
      <Text>task</Text>
    </View>
  );
};

export default task;

const styles = StyleSheet.create({});
